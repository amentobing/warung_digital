---
const { title = "", showTitle = false, category, price = 0, product } = Astro.props;
const harga = price.toLocaleString("id-ID");
---

<a href={`/product/${product}/${category}`} class="bg-white p-6 rounded-lg shadow-md hover:shadow-xl hover:bg-bg-2 transition duration-300 ease-in-out text-center justify-center align-middle flex">
  <div class="flex flex-col justify-center align-middle">
    <h4 class="font-bold text-base uppercase text-secondary mt-2">{title}</h4>
    <p class="text-sm text-secondary opacity-80 p-2">Harga: <span class="font-semibold opacity-100">Rp. {harga}</span></p>
  </div>
</a>
